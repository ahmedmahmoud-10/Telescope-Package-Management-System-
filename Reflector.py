from OTA import OTA

class Reflector(OTA):
    def __init__(self, ota_type, ota_aperture, ota_focal_length, mount_plate_type, weight):
        super().__init__(ota_type, ota_aperture, ota_focal_length, mount_plate_type, weight)
